{
  "name": "$namespace$",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -v && tsc && omniafx build"
  },
  "dependencies": {},
  "devDependencies": {
    "@omnia/tooling-composers": "8.0.362-dev"
  },
  "engines": {
    "node": ">=18.14.0"
  }
}